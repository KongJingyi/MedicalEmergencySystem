import * as Cesium from 'cesium'

export const VEHICLE_TYPES = {
  // 1. ?????????
  DRONE: {
    modelUri: '/models/drone.glb',
    scale: 1.0,         // ???????????????????? 10.0
    minimumPixelSize: 64,
    fixHeading: 0,      // ??????????????????90????????? 90
    pathColor: Cesium.Color.ORANGERED,
    flyHeight: 80       // 默认无人机高度（更贴近楼房上方）
  },
  
  // 2. ?????????
  AMBULANCE: {
    modelUri: '/models/ambulance.glb',
    scale: 3.0,        // ????С???С?С
    minimumPixelSize: 24,
    fixHeading: 90,     // ???????????????? +X ??????????? 90 ??
    pathColor: Cesium.Color.YELLOW,
    flyHeight: 50       // ?????????????????????????
  }
}
